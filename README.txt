修改了Jack YouOSX的YouTube专用hosts，同时参考此hosts添加bigcache.googleapis、googlesyndication、android.clients.google的域名。
整合了imouto.host、netsh.org、huhamhire-hosts、google-hosts中的Google服务（含YouTube）地址（仅收录域名，不收录ip）；
并以DNS服务器ns.center.web.ad.jp为查询依据，删除其中一些无效的域名。
其他网址收录Wiki百科、Dropbox、Facebook、Twitter、Wordpress、Flickr、Ixquick。
